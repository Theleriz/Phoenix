[project]
name = "phoenix-imu-gateway"
version = "0.1.0"
description = "PHOENIX IMU gateway technical scaffold"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
ble = ["bleak>=0.22,<4"]

[tool.unittest]
start-directory = "tests"
